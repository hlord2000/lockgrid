# Machine-readable event trace for the topology visualisation. Logging is turned
# down to warnings so the trace lines are not buried in log output, and immediate
# log mode is dropped because the trace uses printk directly.
CONFIG_LOCKGRID_TRACE=y
CONFIG_LOCKGRID_TRACE_FRAMES=y
CONFIG_LOCKGRID_LOG_LEVEL_WRN=y
