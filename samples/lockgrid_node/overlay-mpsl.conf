# Arbitrate radio access through MPSL timeslots instead of owning the radio, so
# LockGrid can coexist with a Bluetooth LE stack on the same chip.
CONFIG_MPSL=y
CONFIG_LOCKGRID_RADIO_MPSL=y
CONFIG_MPSL_TIMESLOT_SESSION_COUNT=1
